# TEST CASES — CTV Management | Part 4: Edge Cases
**Project:** Empire Community — CTV Management
**Part:** 4 / 4 (Edge Cases bổ sung)
**Total Part 4:** 8 TCs
**Format:** Excel / Jira / TestRail ready
**Updated:** 14/04/2026

---

| TC ID | Module | Risk Level | Test Title | Pre-Condition | Test Steps | Expected Result | Priority | Test Data | Status |
|-------|--------|-----------|-----------|---------------|------------|----------------|----------|-----------|--------|
| EMP_CTV_EC_TC_001 | M1.2 — Role Change | High | CTV đổi role sang Teacher — mất quyền CTV ngay, reply cũ frozen, bị loại khỏi dashboard | - `ctv_x@empire.vn` có role = CTV, status = Active<br>- `ctv_x` đã reply 2 bài: `POST-X1` (`ctv_reply_count=1`), `POST-X2` (`ctv_reply_count=2`)<br>- `admin_01@empire.vn` đăng nhập | 1. Admin đổi role `ctv_x@empire.vn` từ CTV → Teacher<br>2. Kiểm tra Admin Dashboard "Collaborators Management"<br>3. `ctv_x` đăng nhập, vào CTV feed<br>4. `ctv_x` thử gọi API: `POST /api/posts/POST-X3/comments` với `author_role = "ctv"`<br>5. Kiểm tra comment cũ của `ctv_x` tại `POST-X1` và `POST-X2`<br>6. Kiểm tra `ctv_reply_count` của `POST-X1` và `POST-X2` trong DB | 1. Role đổi thành công<br>2. `ctv_x` bị loại khỏi danh sách Collaborators Management (không còn xuất hiện)<br>3. CTV feed không hiển thị, `ctv_x` không còn quyền CTV<br>4. API trả về `403 Forbidden`<br>5. Comment cũ của `ctv_x` vẫn hiển thị trong thread (frozen), badge đổi thành role mới hoặc "Former CTV"<br>6. `ctv_reply_count` của `POST-X1` vẫn = 1, `POST-X2` vẫn = 2 (slot không được trả lại) | High | Email: `ctv_x@empire.vn` / `Pass@1234`<br>Post IDs: `POST-X1`, `POST-X2`, `POST-X3`<br>Admin: `admin_01@empire.vn` / `Admin@5678` | Not Run |
| EMP_CTV_EC_TC_002 | M3.1 — Timing Edge | High | CTV reply thành công rồi bị deactivate trong < 1 giây — reply đã lưu vẫn giữ với badge "CTV (Inactive)" | - `ctv_y@empire.vn` có role = CTV, status = Active<br>- `POST-Y1` có `ctv_reply_count = 0`<br>- `admin_01@empire.vn` sẵn sàng deactivate | 1. `ctv_y` submit reply: `"Reply timing edge case test"` vào `POST-Y1`<br>2. Ngay khi server trả về `200 OK` (reply đã commit), Admin deactivate `ctv_y` trong vòng < 1 giây<br>3. Kiểm tra DB: bảng `comments` và `posts`<br>4. Đăng nhập `student_02@empire.vn`, xem `POST-Y1`<br>5. Kiểm tra badge trên comment của `ctv_y` | 1. Reply submit thành công, nhận `200 OK`<br>2. Admin deactivate thành công, `ctv_y.status = Inactive`<br>3. Comment vẫn tồn tại trong DB với `author_id = ctv_y`. `ctv_reply_count = 1` (slot vẫn bị chiếm)<br>4. Comment của `ctv_y` hiển thị bình thường trong thread<br>5. Badge hiển thị `"CTV (Inactive)"`, tên `ctv_y` vẫn hiện | High | Email: `ctv_y@empire.vn` / `Pass@1234`<br>Post ID: `POST-Y1`<br>Content: `"Reply timing edge case test"`<br>Admin: `admin_01@empire.vn` / `Admin@5678` | Not Run |
| EMP_CTV_EC_TC_003 | M3.1 — Session Expired | Medium | Session expired trong khi CTV đang soạn reply — xử lý graceful, không mất content | - `ctv_z@empire.vn` đang đăng nhập, token sắp hết hạn (còn < 30 giây)<br>- `POST-Z1` có `ctv_reply_count = 0` | 1. `ctv_z` mở `POST-Z1`, bắt đầu soạn reply: `"Nội dung reply quan trọng cần được lưu lại sau khi session hết hạn"`<br>2. Để session hết hạn tự nhiên (hoặc invalidate token từ server)<br>3. `ctv_z` click Submit<br>4. Quan sát UI response và hành vi hệ thống<br>5. `ctv_z` đăng nhập lại<br>6. Kiểm tra nội dung đang soạn có được giữ không | 1. Reply đang được soạn bình thường<br>2. Session hết hạn<br>3. Hệ thống trả về `401 Unauthorized` hoặc redirect đến trang login. Không trả về `200 OK` giả<br>4. UI hiển thị thông báo rõ ràng: `"Phiên đăng nhập đã hết hạn. Vui lòng đăng nhập lại"`. Nội dung đang soạn được giữ lại trong local storage hoặc hiển thị lại sau login (nếu có)<br>5. Đăng nhập thành công<br>6. Nếu có draft preservation: content cũ xuất hiện lại. Nếu không: UI thông báo rõ để user biết phải nhập lại | Medium | Email: `ctv_z@empire.vn` / `Pass@1234`<br>Post ID: `POST-Z1`<br>Draft content: `"Nội dung reply quan trọng cần được lưu lại sau khi session hết hạn"` | Not Run |
| EMP_CTV_EC_TC_004 | M2.1 — Feed After Own Reply | High | CTV đã reply 1 lần (chiếm slot) — bài có còn hiển thị trong CTV feed không | - `ctv_a@empire.vn` đăng nhập, status = Active<br>- `POST-AA` có `ctv_reply_count = 0`<br>- `ctv_a` chưa reply `POST-AA` | 1. `ctv_a` vào CTV feed — xác nhận thấy `POST-AA`<br>2. `ctv_a` reply `POST-AA`: `"Reply lần đầu từ CTV_A"` → submit thành công<br>3. Kiểm tra `ctv_reply_count` trong DB (phải = 1)<br>4. `ctv_a` quay lại CTV feed<br>5. Tìm `POST-AA` trong feed của `ctv_a`<br>6. `ctv_a` gọi API `GET /api/ctv/feed` và kiểm tra response | 1. `POST-AA` hiển thị trong feed (count=0)<br>2. Reply thành công<br>3. `ctv_reply_count = 1`<br>4. Feed load lại<br>5. `POST-AA` vẫn hiển thị trong feed của `ctv_a` (bài còn 1 slot trống, và `ctv_a` đã chiếm slot → theo business rule: bài có `count < 2` vẫn hiện)<br>6. API response vẫn chứa `POST-AA` vì `ctv_reply_count = 1 < 2`<br>**Lưu ý:** Nếu kết quả khác với expected → cần escalate để confirm business rule | High | Email: `ctv_a@empire.vn` / `Pass@1234`<br>Post ID: `POST-AA`<br>Content: `"Reply lần đầu từ CTV_A"` | Not Run |
| EMP_CTV_EC_TC_005 | M6.1 — Deleted Post | Medium | Bài bị xóa sau khi CTV đã reply — CTV Personal Dashboard xử lý graceful | - `ctv_a@empire.vn` đã reply `POST-DEL2`<br>- `POST-DEL2` sau đó bị xóa khỏi DB (cascade delete replies)<br>- `ctv_a` đăng nhập | 1. `ctv_a` vào Personal Dashboard<br>2. Tìm `POST-DEL2` trong danh sách bài đã reply<br>3. Nếu vẫn hiển thị, click "View Detail" hoặc click link bài<br>4. Kiểm tra DB: bảng `comments` — các reply của `ctv_a` trên `POST-DEL2` | 1. Personal Dashboard load bình thường<br>2. `POST-DEL2` không còn trong danh sách (đã bị cascade delete) HOẶC hiển thị với label `"[Bài đã bị xóa]"` — không crash<br>3. Nếu click: hiển thị message `"Bài viết này không còn tồn tại"`, không có 500 error<br>4. Các reply của `ctv_a` trên `POST-DEL2` đã bị cascade delete khỏi DB | Medium | Email: `ctv_a@empire.vn` / `Pass@1234`<br>Post ID: `POST-DEL2` (đã bị xóa) | Not Run |
| EMP_CTV_EC_TC_006 | M3.3 — Race Condition + Deactivate | High | Admin deactivate CTV đang trong giữa race condition — counter không bị corrupt | - `POST-RC` có `ctv_reply_count = 1` (CTV_Ref đã reply)<br>- `ctv_race1@empire.vn` và `ctv_race2@empire.vn` cả 2 đang active, chưa reply `POST-RC`<br>- `admin_01@empire.vn` sẵn sàng | 1. `ctv_race1` và `ctv_race2` cùng soạn reply cho `POST-RC`<br>2. Trigger đồng thời: `ctv_race1` submit reply + Admin deactivate `ctv_race2` trong vòng < 200ms<br>3. Thu thập response của `ctv_race1`<br>4. Kiểm tra DB: `ctv_reply_count` của `POST-RC`<br>5. Kiểm tra DB: số comment records với `author_role = "ctv"` trên `POST-RC`<br>6. Nếu `ctv_race2` đã kịp submit trước khi bị deactivate: kiểm tra reply đó trong DB | 1. Actions được trigger đồng thời<br>2. Hệ thống xử lý không deadlock<br>3. `ctv_race1` nhận `200 OK` hoặc `403` (tùy timing)<br>4. `ctv_reply_count` = 2 (không phải 3 hoặc 0)<br>5. Chỉ có đúng số reply hợp lệ trong DB — không có orphan records<br>6. Nếu `ctv_race2` reply trước khi bị deactivate: reply được lưu, badge = `"CTV (Inactive)"` | High | Post ID: `POST-RC`<br>Emails: `ctv_race1@empire.vn`, `ctv_race2@empire.vn` / `Pass@1234`<br>Admin: `admin_01@empire.vn` / `Admin@5678` | Not Run |
| EMP_CTV_EC_TC_007 | M5.1 — Notification Race | Medium | Notification đã được enqueue, CTV bị deactivate trước khi nhận — không gửi đến CTV inactive | - `POST-NR` đang ở trạng thái `pending_review`<br>- `ctv_notif@empire.vn` đang Active<br>- `admin_01@empire.vn` đăng nhập | 1. Admin bắt đầu approve `POST-NR`<br>2. Ngay trong quá trình approve (trước khi notification được dispatch), Admin deactivate `ctv_notif@empire.vn`<br>3. Hoàn tất approve `POST-NR`<br>4. Kiểm tra notification inbox của `ctv_notif`<br>5. Kiểm tra notification của các CTV active khác | 1. Approve bắt đầu<br>2. Deactivate `ctv_notif` thành công<br>3. `POST-NR` status = published<br>4. `ctv_notif` (Inactive) KHÔNG nhận notification về `POST-NR` (hoặc nếu đã enqueue thì không được deliver)<br>5. CTV active khác vẫn nhận notification bình thường | Medium | Post ID: `POST-NR`<br>Email: `ctv_notif@empire.vn`<br>Admin: `admin_01@empire.vn` / `Admin@5678` | Not Run |
| EMP_CTV_EC_TC_008 | M3.2 — Slot After Role Change | High | ctv_reply_count không thay đổi sau khi CTV đổi role — slot vẫn bị chiếm | - `ctv_x@empire.vn` có role = CTV (đã đổi role ở TC-EC-001 hoặc tạo mới)<br>- `POST-RS` có `ctv_reply_count = 1` (do `ctv_x` đã reply khi còn là CTV)<br>- `ctv_new@empire.vn` có role = CTV, status = Active, chưa reply `POST-RS` | 1. Xác nhận `ctv_x` đã reply `POST-RS` khi còn là CTV (`ctv_reply_count = 1`)<br>2. Admin đổi role `ctv_x` → Teacher<br>3. Kiểm tra `ctv_reply_count` của `POST-RS` trong DB<br>4. `ctv_new` vào CTV feed, tìm `POST-RS`<br>5. `ctv_new` reply `POST-RS`: `"CTV mới reply sau khi CTV cũ đổi role"`<br>6. Kiểm tra `ctv_reply_count` sau khi `ctv_new` reply | 1. `ctv_reply_count = 1`<br>2. Role đổi thành công<br>3. `ctv_reply_count` vẫn = 1 (slot của `ctv_x` không được trả lại dù đã đổi role)<br>4. `POST-RS` vẫn hiển thị trong CTV feed của `ctv_new` (count = 1 < 2)<br>5. Reply thành công<br>6. `ctv_reply_count = 2`. `POST-RS` biến mất khỏi CTV feed | High | Email: `ctv_x@empire.vn` (role đã đổi sang Teacher)<br>Email: `ctv_new@empire.vn` / `Pass@1234`<br>Post ID: `POST-RS`<br>Content: `"CTV mới reply sau khi CTV cũ đổi role"` | Not Run |
